// Topic 4: filter()

// This is one of the most used methods in React.
// Returns all matching elements.

const numbers = [10, 20, 30, 40, 50];

const result = numbers.filter((number) => number >= 30);
console.log(result);
